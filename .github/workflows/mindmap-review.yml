name: Archived iSCARB mind-map v1 review
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Mind-map v1 checks are archived. Story-v2 review verifies the current concept map, case thread, endings, navigation and offline packages."
