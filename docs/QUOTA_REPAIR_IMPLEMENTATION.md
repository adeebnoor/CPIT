Implementation summary: `_major_coverage_gaps` detects missing/late major P1 checkpoints. `_ensure_quota_safe_completeness` preserves a complete semantic Blueprint or replaces an incomplete one with the deterministic source-bounded Blueprint, recomputes local gates, and forces a non-passing fallback audit. The repair-quota and end-of-run semantic-unavailable paths both use this guard.